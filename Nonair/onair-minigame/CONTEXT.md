## 🧠 CONTEXT.md - ON:AIR 미니 게임

이 문서는 **ON:AIR 미니 게임** 프로젝트의 규칙, 아키텍처 및 코딩 표준을 정의합니다. 모든 코드 변경 및 생성은 아래 지침을 엄격하게 준수해야 합니다.

-----

## 🏗 프로젝트 아키텍처

### **핵심 정체성**

  * **유형:** AI 기반 웹 미니 게임
  * **목표:** 부담 없이 노출 치료를 통해 전화 공포증(Callphobia)을 완화.
  * **톤:** 부드러움, 격려, 안전함, 느린 속도.

### **기술 스택**

  * **백엔드:** Python 3.10+, FastAPI
  * **프론트엔드:** HTML5, CSS3, 바닐라 자바스크립트 (V1에서는 무거운 프레임워크 사용 안 함)
  * **AI 엔진:** Google Gemini API (또는 OpenAI 호환)
  * **통신:** REST API (JSON)

### **디렉토리 구조**

```
/onair-mini-game
├── src/
│   ├── main.py              # FastAPI 앱 진입점
│   ├── llm/                 # LLM 상호작용
│   ├── utils/               # 프롬프트 및 헬퍼 함수
│   └── frontend/            # 정적 파일 (HTML/JS/CSS)
├── tests/                   # 단위 테스트
└── .env                     # 보안 정보 (커밋 금지)
```

-----

## 👨‍💻 코딩 표준

### **Python (백엔드)**

  * **스타일:** PEP 8 준수.
  * **타입:** 모든 함수 인자와 반환 값에 **`타입 힌트`** 사용.
  * **비동기:** 모든 I/O 바운드 작업(특히 LLM API 호출)에는 **`async/await`** 사용.
  * **Docstrings:** 모든 API 엔드포인트 및 복잡한 로직에 필요.

### **JavaScript (프론트엔드)**

  * **스타일:** 최신 ES6+ 문법 (`const`, `let`, 화살표 함수).
  * **jQuery 금지:** 바닐라 JS DOM 조작 사용.
  * **Fetch:** 백엔드 통신에 `fetch` API 사용.

### **CSS (디자인)**

  * **미학:** 따뜻하고 부드러운 색상 (**파스텔 톤**).
  * **애니메이션:** 부드러운 전환 (예: 전화 벨소리).
  * **반응형:** **모바일 우선** 디자인 권장.

-----

## 🤖 LLM 생성 규칙 (바이브 코딩)

1.  **안전 제일**: AI 응답은 절대 거칠거나, 요구하거나, 부정적이지 않아야 합니다. 항상 공손하고 지지하는 태도를 유지해야 합니다.
2.  **결정론적 구조**: 구문 분석 오류를 방지하기 위해 가능한 경우 **구조화된 JSON**을 반환하도록 프롬프트를 설정합니다.
3.  **모의(Mock) 우선**: API 키가 없을 때는 견고한 **모의 모드**를 제공하여 게임을 즉시 플레이할 수 있도록 합니다.

-----

## 📝 주요 파일 설명

  * `prompts.py`: LLM 페르소나(시나리오 생성자, 상담 친구)를 위한 시스템 명령을 포함합니다.
  * `main.py`: 백엔드의 핵심. 트래픽을 라우팅합니다.
  * `game.js`: UI 상태(벨 울림 -\> 옵션 -\> 피드백)를 처리합니다.