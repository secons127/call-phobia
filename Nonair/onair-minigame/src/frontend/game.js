document.addEventListener('DOMContentLoaded', () => {
    const startBtn = document.getElementById('btn-start');

    startBtn.addEventListener('click', startGame);

    function startGame() {
        alert("게임 로직은 아직 구현되지 않았습니다!");
        // TODO: Call API to get scenario
    }
});
