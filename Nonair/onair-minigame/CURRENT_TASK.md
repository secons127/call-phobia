# 🔨 CURRENT TASK: Frontend Integration

## 🎯 Objective
Connect the frontend `game.js` to the implemented backend APIs (`/api/scenario` and `/api/feedback`).

## 📋 To-Do List
- [ ] Implement `fetchScenario()` in `src/frontend/game.js`
- [ ] Implement `sendChoice(choice)` in `src/frontend/game.js`
- [ ] Implement UI state updates (Ringing -> Scenario -> Feedback)
- [ ] Add loading states and error handling

## 🚀 Next Step
After frontend integration, proceed to **Verification & Polish** testing the full flow.
